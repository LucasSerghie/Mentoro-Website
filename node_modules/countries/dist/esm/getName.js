import { continents } from './continents';
var dictionary = {};
/**
 *
 * @param abbreviation
 */
export var getName = function (key) {
    if (dictionary[key] == null) {
        for (var _i = 0, _a = Object.entries(continents); _i < _a.length; _i++) {
            var entry = _a[_i];
            var countries = entry[1];
            for (var _b = 0, countries_1 = countries; _b < countries_1.length; _b++) {
                var country = countries_1[_b];
                var abbr = country.abbr, name_1 = country.name, ioc = country.ioc;
                dictionary[ioc] = name_1;
                dictionary[abbr] = name_1;
            }
        }
    }
    return dictionary[key];
};
//# sourceMappingURL=getName.js.map